// Not a plugin. This file exists only for packaging: windeployqt bundles the
// QML modules referenced by --qmldir sources, and user plugins can import any
// of these at runtime. Add imports here if your users need more modules.
import QtQuick
import QtQuick.Window
import QtQuick.Controls
import QtQuick.Layouts

Item {}
